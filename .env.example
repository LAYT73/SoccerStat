# Proxy URL for football data API (to avoid CORS issues in production)
VITE_FOOTBALL_DATA_API_URL=https://soccerstat-proxy.nsshipilov.workers.dev/api

# Public base path for the client bundle. Use / for Docker/nginx and /SoccerStat/ for GitHub Pages.
VITE_APP_BASE_PATH=/
